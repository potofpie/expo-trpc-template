{
    "name": "@expo-template/database",
    "version": "0.0.0",
    "type": "module",
    "main": "./src/index.ts",
    "scripts": {
        "dev": "bun run src/index.ts",
        "build": "bun build src/index.ts --outdir ./dist",
        "test": "bun test",
        "migrate": "bun run src/migrate.ts",
        "generate": "drizzle-kit generate",
        "push": "drizzle-kit push"
    },
    "dependencies": {
        "drizzle-orm": "^0.45.0"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "drizzle-kit": "^0.31.8"
    }
}